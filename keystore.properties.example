# Copy to keystore.properties (gitignored) for local release builds.
# CI uses the TECHYST_* environment variables instead; see .github/workflows/release.yml.
storeFile=/absolute/path/to/techyst-cloud-upload.jks
storePassword=
keyAlias=techyst-cloud-upload
keyPassword=
